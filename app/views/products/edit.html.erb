<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        <h1>Edit <%= @product.name %></h1>

        <%= render partial: 'form', locals: { product: @product, departments: @departments, store: @store, user: current_user } %>
    </div>
</div>